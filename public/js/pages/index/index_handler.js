function indexHandler() {
    const st = 'selected';

    productCardHandler(st);

    navbarClickHandler(st);

    bascketHandler();

    // setTimeout(function() {
    //     alertCookie();
    // }, 1000)
}

indexHandler();